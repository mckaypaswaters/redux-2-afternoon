// INITIAL STATE
const initialState = {
    purchases: [],
    budgetLimit: null,
    loading: false
}

// ACTION CONSTS

// ACTION BUILDERS

// REDUCER
export default function reducer(state = initialState, action){
    switch(action.type){
        default: return state
    }
}