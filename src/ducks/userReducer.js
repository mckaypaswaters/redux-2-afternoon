// INITIAL STATE
const initialState = {
    email: null,
    firstName: null,
    lastName: null
}
// ACTION CONSTS

// ACTION BUILDERS

// REDUCER
export default function reducer(state = initialState, action){
    switch(action.type){
        default: return state
    }
}